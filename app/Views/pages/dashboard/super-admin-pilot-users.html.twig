<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="mb-0"><i class="bi bi-people"></i> Users Piloto</h1>
                <a href="{{ BASE_URL }}dashboard/super-admin" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Voltar ao Dashboard
                </a>
            </div>

            {% if messages.success %}
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i>{{ messages.success }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            {% endif %}

            {% if messages.error %}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>{{ messages.error }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            {% endif %}

            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="bi bi-envelope-heart"></i> Lista de Users Piloto</h5>
                </div>
                <div class="card-body">
                    {% if pilot_users and pilot_users|length > 0 %}
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>Email</th>
                                    <th>Data de Inscrição</th>
                                    <th>Status</th>
                                    <th>Convites</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for pilot in pilot_users %}
                                <tr>
                                    <td>
                                        <strong>{{ pilot.email }}</strong>
                                        {% if pilot.has_registered %}
                                        <br><small class="text-success"><i class="bi bi-check-circle"></i> Já registado</small>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {{ pilot.subscribed_at|date('d/m/Y H:i') }}
                                    </td>
                                    <td>
                                        {% if pilot.has_registered %}
                                        <span class="badge bg-success">Registado</span>
                                        {% elseif pilot.has_pending_invite %}
                                        <span class="badge bg-warning">Convite Pendente</span>
                                        {% else %}
                                        <span class="badge bg-secondary">Aguardando Convite</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if pilot.tokens|length > 0 %}
                                        <div class="small">
                                            <strong>Total:</strong> {{ pilot.tokens|length }}<br>
                                            <span class="text-success">Usados:</span> {{ pilot.used_invites|default(0) }}<br>
                                            <span class="text-warning">Pendentes:</span> {{ pilot.pending_invites|default(0) }}
                                            {% if pilot.last_invite_sent %}
                                            <br><small class="text-muted">Último: {{ pilot.last_invite_sent|date('d/m/Y') }}</small>
                                            {% endif %}
                                        </div>
                                        {% else %}
                                        <span class="text-muted">Nenhum</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if not pilot.has_registered %}
                                        <form method="POST" action="{{ BASE_URL }}admin/pilot-users/send-invite" class="d-inline">
                                            <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                                            <input type="hidden" name="email" value="{{ pilot.email }}">
                                            <button type="submit" class="btn btn-primary btn-sm" onclick="return confirm('Tem certeza que deseja enviar um convite de registo para {{ pilot.email }}?');">
                                                <i class="bi bi-send"></i> Enviar Convite
                                            </button>
                                        </form>
                                        {% else %}
                                        <span class="text-muted">Já registado</span>
                                        {% endif %}
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    {% else %}
                    <div class="text-center py-5">
                        <i class="bi bi-inbox" style="font-size: 3rem; color: #ccc;"></i>
                        <p class="text-muted mt-3">Nenhum user piloto encontrado.</p>
                    </div>
                    {% endif %}
                </div>
            </div>

            {% if pilot_users and pilot_users|length > 0 %}
            <div class="card mt-4 shadow-sm">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0"><i class="bi bi-info-circle"></i> Informações</h5>
                </div>
                <div class="card-body">
                    <ul class="mb-0">
                        <li>Os users piloto são pessoas que se inscreveram através do modal de fase de testes.</li>
                        <li>Pode enviar convites de registo que permitem criar conta mesmo com registo desativado.</li>
                        <li>Os convites expiram após 7 dias e só podem ser usados uma vez.</li>
                        <li>Quando um user se registar usando o convite, aparecerá como "Registado" na lista.</li>
                    </ul>
                </div>
            </div>
            {% endif %}
        </div>
    </div>
</div>
