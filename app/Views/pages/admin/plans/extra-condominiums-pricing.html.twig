<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2>Preços de Condomínios Extras</h2>
            <p class="text-muted mb-0">Plano: <strong>{{ plan.name }}</strong></p>
        </div>
        <div>
            <a href="{{ BASE_URL }}admin/plans/{{ plan.id }}/extra-condominiums-pricing/create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Criar Novo Escalão
            </a>
            <a href="{{ BASE_URL }}admin/plans" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Voltar aos Planos
            </a>
        </div>
    </div>

    {% if error %}
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        {{ error }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    {% endif %}

    {% if success %}
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        {{ success }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    {% endif %}

    <div class="card shadow-sm mb-4">
        <div class="card-header bg-info text-white">
            <h5 class="mb-0"><i class="bi bi-info-circle"></i> Informação</h5>
        </div>
        <div class="card-body">
            <p class="mb-0">Os preços dos condomínios extras ficam mais baratos conforme o número de condomínios subscritos aumenta. Configure os escalões abaixo para definir os preços por condomínio extra em cada faixa de condomínios.</p>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0"><i class="bi bi-list-ul"></i> Escalões de Preço</h5>
        </div>
        <div class="card-body">
            {% if pricing_tiers %}
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Ordem</th>
                            <th>Condomínios Mín.</th>
                            <th>Condomínios Máx.</th>
                            <th>Preço por Condomínio</th>
                            <th>Status</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for tier in pricing_tiers %}
                        <tr>
                            <td>{{ tier.sort_order }}</td>
                            <td><strong>{{ tier.min_condominios }}</strong></td>
                            <td>
                                {% if tier.max_condominios %}
                                    {{ tier.max_condominios }}
                                {% else %}
                                    <span class="text-muted">Ilimitado</span>
                                {% endif %}
                            </td>
                            <td><strong class="text-success">€{{ tier.price_per_condominium|number_format(2, ',', '.') }}</strong></td>
                            <td>
                                {% if tier.is_active %}
                                    <span class="badge bg-success">Ativo</span>
                                {% else %}
                                    <span class="badge bg-secondary">Inativo</span>
                                {% endif %}
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a href="{{ BASE_URL }}admin/plans/{{ plan.id }}/extra-condominiums-pricing/{{ tier.id }}/edit" class="btn btn-sm btn-info" title="Editar">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <form method="POST" action="{{ BASE_URL }}admin/plans/{{ plan.id }}/extra-condominiums-pricing/{{ tier.id }}/toggle-active" class="d-inline">
                                        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                                        <button type="submit" class="btn btn-sm {% if tier.is_active %}btn-warning{% else %}btn-success{% endif %}" title="{% if tier.is_active %}Desativar{% else %}Ativar{% endif %}">
                                            <i class="bi {% if tier.is_active %}bi-toggle-on{% else %}bi-toggle-off{% endif %}"></i>
                                        </button>
                                    </form>
                                    <button type="button" class="btn btn-sm btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal{{ tier.id }}" title="Deletar">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>

                        <!-- Delete Modal -->
                        <div class="modal fade" id="deleteModal{{ tier.id }}" tabindex="-1" aria-labelledby="deleteModalLabel{{ tier.id }}" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <div class="modal-header bg-danger text-white">
                                        <h5 class="modal-title" id="deleteModalLabel{{ tier.id }}">Deletar Escalão</h5>
                                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <form method="POST" action="{{ BASE_URL }}admin/plans/{{ plan.id }}/extra-condominiums-pricing/{{ tier.id }}/delete">
                                        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                                        <div class="modal-body">
                                            <p>Tem certeza que deseja deletar este escalão de preço?</p>
                                            <p><strong>Faixa:</strong> {{ tier.min_condominios }} - {% if tier.max_condominios %}{{ tier.max_condominios }}{% else %}Ilimitado{% endif %} condomínios</p>
                                            <p><strong>Preço:</strong> €{{ tier.price_per_condominium|number_format(2, ',', '.') }} por condomínio</p>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                            <button type="submit" class="btn btn-danger">Deletar</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {% else %}
            <div class="text-center text-muted py-5">
                <i class="bi bi-inbox" style="font-size: 3rem;"></i>
                <p class="mt-3">Nenhum escalão de preço configurado ainda.</p>
                <a href="{{ BASE_URL }}admin/plans/{{ plan.id }}/extra-condominiums-pricing/create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Criar Primeiro Escalão
                </a>
            </div>
            {% endif %}
        </div>
    </div>
</div>
