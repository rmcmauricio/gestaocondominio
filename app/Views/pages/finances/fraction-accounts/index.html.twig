<div class="container py-4">
    {% set page_actions %}
        <div class="page-header-actions">
            <a href="{{ BASE_URL }}condominiums/{{ condominium.id }}/finances" class="btn btn-outline-secondary btn-sm">
                <i class="bi bi-arrow-left"></i> Finanças
            </a>
        </div>
    {% endset %}
    {% include 'blocks/page-header.html.twig' with {
        'title': 'Contas por fração',
        'actions': page_actions
    } %}

    {% if error %}
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        {{ error }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fechar"></button>
    </div>
    {% endif %}
    {% if success %}
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        {{ success }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fechar"></button>
    </div>
    {% endif %}

    <p class="text-muted mb-4">Saldo da conta (pagamentos menos quotas liquidadas), valor em falta nas quotas — todos os anos e dívidas históricas — e situação (saldo − em falta). Clique na fração para ver o histórico.</p>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Fração</th>
                            <th class="text-end">Saldo conta</th>
                            <th class="text-end">Em falta</th>
                            <th class="text-end">Situação</th>
                            <th class="text-center" style="width: 100px;">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for row in rows %}
                        <tr>
                            <td><strong>{% include 'blocks/fraction-popover.html.twig' with { identifier: row.fraction.identifier, owner_name: row.owner_name, floor: row.floor } only %}</strong></td>
                            <td class="text-end">
                                <span class="{% if row.balance > 0 %}text-success{% elseif row.balance < 0 %}text-danger{% else %}text-muted{% endif %} fw-bold">
                                    €{{ row.balance|number_format(2, ',', '.') }}
                                </span>
                            </td>
                            <td class="text-end">
                                <span class="{% if row.due > 0 %}text-danger{% else %}text-muted{% endif %}">
                                    €{{ row.due|number_format(2, ',', '.') }}
                                </span>
                            </td>
                            <td class="text-end">
                                <span class="{% if row.situacao > 0 %}text-success{% elseif row.situacao < 0 %}text-danger{% else %}text-muted{% endif %} fw-bold">
                                    €{{ row.situacao|number_format(2, ',', '.') }}
                                </span>
                            </td>
                            <td class="text-center">
                                <a href="{{ BASE_URL }}condominiums/{{ condominium.id }}/fraction-accounts/{{ row.fraction_id }}" class="btn btn-sm btn-outline-primary" title="Ver detalhe">
                                    <i class="bi bi-eye"></i>
                                </a>
                            </td>
                        </tr>
                        {% else %}
                        <tr>
                            <td colspan="5" class="text-center text-muted py-4">Nenhuma fração encontrada.</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ BASE_URL }}condominiums/{{ condominium.id }}/finances" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Voltar às Finanças
        </a>
    </div>
</div>
<script>
document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('.fraction-popover').forEach(function(el) { new bootstrap.Popover(el, { trigger: 'focus' }); });
});
</script>
