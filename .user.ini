; Session: keep active 24 hours after last user interaction
; Used by PHP when running this app (e.g. PHP-FPM). Ensures server-side session
; data is not garbage-collected before 24h even if another script starts the session.
session.gc_maxlifetime = 86400
